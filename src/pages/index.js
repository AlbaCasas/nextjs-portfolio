import Layout from "../components/Layout";
import Home from "../views/Home";

const HomePage = () => {
  return (
    <Layout isFullScreen>
      <Home />
    </Layout>
  );
};

export default HomePage;
